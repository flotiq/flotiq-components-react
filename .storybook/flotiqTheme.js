import { create } from '@storybook/theming';

export default create({
    base: 'light',
    brandTitle: 'Flotiq components for React',
    brandUrl: 'https://flotiq.com',    
    fontBase: '"Inter", sans-serif',
});
